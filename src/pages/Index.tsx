import { ProductDetailPage } from "@/components/ProductDetailPage";
import { mockProduct } from "@/data/mockProduct";

const Index = () => {
  return <ProductDetailPage product={mockProduct} />;
};

export default Index;
